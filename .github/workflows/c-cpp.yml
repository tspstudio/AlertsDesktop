name: C/C++ CI

on:
  push:
    branches: [ "dev" ]
  pull_request:
    branches: [ "dev" ]

jobs:
  build:

    runs-on: any-linux

    steps:
    - uses: actions/checkout@v4
    - name: cmake
      run: cmake build . && make
